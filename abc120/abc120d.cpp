#include <iostream>
#include <string>
using namespace std;

int main()
{
    int N, M;

    cin >> N >> M;

    int A[M], B[M];

    for (int i = 1; i <= M; i++)
    {
        cin >> A[i] >> B[i];
    }

    int inconvenience;

    return 0;
}