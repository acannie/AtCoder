#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long int N, M;

    cin >> N >> M;

    long long int a[N];

    for (long long int i = 0; i < N; i++)
    {
        cin >> a[i];
    }

    sort(a, a + N);

    long long int happy = 0;

    

    

    cout << happy << endl;

    return 0;
}
