#include <iostream>
#include <string>
#include <algorithm>
#include <cmath>
#include <vector>
using namespace std;

int main()
{
    int N;

    cin >> N;

    int u, v;

    cin >> u >> v;

    int A[N - 1], B[N - 1];
    int count[N] = {};

    for (int i = 0; i < N - 1; i++)
    {
        cin >> A[i] >> B[i];
    }

    

    

    //cout << << endl;

    return 0;
}