#include <iostream>
#include <stdio.h>
#include <string>
#include <algorithm>
#include <cmath>
#include <limits.h>
using namespace std;

int main()
{
    long int A;

    scanf("%ld", &A);

    long int ans = A / 2;

    std::cout << A << endl;
    printf("%ld\n", A);
    printf("%ld\n", LONG_MAX);
    return 0;
}
